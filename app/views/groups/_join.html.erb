  <%= form_for @membership, builder: FoundationFormBuilder, :url => url_for(:controller => 'memberships', :action => 'create')  do |f| %> 
		<%= f.hidden_field :group_id, :value => @group.id %>
		<%= f.hidden_field :user_id, :value => @user.id %>
		<%= f.submit "Request to Join"%>
	<% end %>